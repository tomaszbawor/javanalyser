# Test configuration
spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.model=test-model
spring.ai.ollama.embedding.model=test-embedding-model

# Database config is provided dynamically in the test class
# using TestContainers like in VectorEmbeddingRepositoryTest

# JPA Config
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Disable actual code parsing during tests
code.project.path=src/test/resources
